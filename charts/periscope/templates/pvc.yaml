# SQLite history lives here so it survives pod restarts.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: periscope-data
  namespace: periscope
  labels:
    app.kubernetes.io/name: periscope
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
