// Shared by the About page and the masthead. The version is not here: it is
// stamped into the server binary and read from /api/version at runtime.
export const REPO_URL = 'https://github.com/croz-ltd/periscope'
export const ISSUES_URL = `${REPO_URL}/issues`
